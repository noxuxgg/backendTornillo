services:
  db:
    image: postgres:16
    container_name: dbTornillo
    environment:
      POSTGRES_DB: dbTornillo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgresql
    ports:
      - '5437:5432'
    volumes:
      - ./postgres_data:/var/lib/postgresql/data